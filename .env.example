# Public control-plane URL only. Never put credentials in WXT_ variables.
WXT_PREVIEW_API_BASE_URL=http://127.0.0.1:8787
# Production extension build (public configuration only; no GitHub PAT):
# WXT_PREVIEW_API_BASE_URL=https://api.3.34.33.24.sslip.io/
# WXT_PREVIEW_ARTIFACT_BASE_DOMAIN=3.34.33.24.nip.io

# Server-only examples. Keep real values in .env.local or deployment secrets.
PEEPHOLE_API_HOST=127.0.0.1
PEEPHOLE_API_PORT=8787
PEEPHOLE_DATABASE_URL=postgres://peephole:replace-me@localhost:5432/peephole
PEEPHOLE_DATABASE_POOL_SIZE=10

# Optional: raises the server's own public-repository GitHub API rate limit.
# This credential is unrelated to end-user identity and remains server-only.
# PEEPHOLE_GITHUB_TOKEN=ghp_replace-me

# Server-only GitHub App identity configuration. Register callback URL
# /v1/auth/github/callback in the GitHub App. The extension ID allowlist is
# comma-separated; each accepted browser callback is derived server-side as
# https://<allowed-extension-id>.chromiumapp.org/github.
PEEPHOLE_GITHUB_APP_CLIENT_ID=Iv1.replace-me
PEEPHOLE_GITHUB_APP_CLIENT_SECRET=replace-with-github-app-client-secret
PEEPHOLE_GITHUB_APP_CALLBACK_URL=http://127.0.0.1:8787/v1/auth/github/callback
PEEPHOLE_ALLOWED_EXTENSION_IDS=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
PEEPHOLE_GITHUB_OAUTH_STATE_SECRET=replace-with-a-random-32-plus-byte-string

# Signs 30-minute Peephole access sessions. Production must set a stable,
# random value of at least 32 bytes so restarts do not invalidate all users.
PEEPHOLE_SESSION_SIGNING_SECRET=replace-with-a-random-32-plus-byte-string

# Production trusted site root (operator contract, no public suffix lookup).
# Trusted and artifact domains must not be equal or subdomains of each other.
PEEPHOLE_TRUSTED_REGISTRABLE_DOMAIN=peephole.dev
PEEPHOLE_TRUSTED_APP_ORIGIN=https://app.peephole.dev
PEEPHOLE_ARTIFACT_BASE_DOMAIN=peepholeusercontent.dev

# Free development/test deployment example (replace the three values above):
# PEEPHOLE_TRUSTED_REGISTRABLE_DOMAIN=sslip.io
# PEEPHOLE_TRUSTED_APP_ORIGIN=https://app.3.34.33.24.sslip.io
# PEEPHOLE_ARTIFACT_BASE_DOMAIN=3.34.33.24.nip.io
# Public API URL for this deployment: https://api.3.34.33.24.sslip.io
# Trusted app origin accepts HTTPS only, without credentials, explicit port,
# path, query, or fragment; its hostname must be within the trusted site root.
